---
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/docs', label: 'Docs' },
  { href: '/api-coverage', label: 'API Coverage' },
  { href: '/quickstart', label: 'Quick Start' },
  { href: '/changelog', label: 'Changelog' },
  { href: '/about', label: 'About' },
];

const currentPath = Astro.url.pathname;

// Helper to check if current path matches nav item
const isActive = (href: string) => {
  if (href === '/') return currentPath === '/';
  if (href === '/docs') return currentPath.startsWith('/docs');
  return currentPath === href || currentPath === href + '/';
};
---

<header class="fixed top-0 left-0 right-0 z-50 bg-slate-950/80 backdrop-blur-md border-b border-slate-800">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-3 group">
        <span class="text-2xl group-hover:scale-110 transition-transform">🌼</span>
        <span class="font-display font-bold text-lg text-white group-hover:text-rapeseed-400 transition-colors">
          RAPS
        </span>
      </a>
      
      <!-- Desktop Navigation -->
      <nav class="hidden md:flex items-center gap-1">
        {navItems.map((item) => (
          <a
            href={item.href}
            class:list={[
              'px-4 py-2 rounded-lg text-sm font-medium transition-all',
              isActive(item.href)
                ? 'bg-slate-800 text-white'
                : 'text-slate-400 hover:text-white hover:bg-slate-800/50',
              item.label === 'Quick Start' && !isActive(item.href) && 'text-rapeseed-400 hover:text-rapeseed-300',
              item.label === 'Docs' && !isActive(item.href) && 'text-emerald-400 hover:text-emerald-300',
              item.label === 'API Coverage' && !isActive(item.href) && 'text-purple-400 hover:text-purple-300',
            ]}
          >
            {item.label}
          </a>
        ))}
      </nav>
      
      <!-- CTA Buttons -->
      <div class="flex items-center gap-3">
        <a
          href="https://github.com/dmytro-yemelianov/raps"
          target="_blank"
          rel="noopener noreferrer"
          class="hidden sm:flex items-center gap-2 px-3 py-1.5 text-sm text-slate-400 hover:text-white transition-colors"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path fill-rule="evenodd" clip-rule="evenodd" d="M12 2C6.477 2 2 6.477 2 12c0 4.42 2.87 8.17 6.84 9.5.5.08.66-.23.66-.5v-1.69c-2.77.6-3.36-1.34-3.36-1.34-.46-1.16-1.11-1.47-1.11-1.47-.91-.62.07-.6.07-.6 1 .07 1.53 1.03 1.53 1.03.87 1.52 2.34 1.07 2.91.83.09-.65.35-1.09.63-1.34-2.22-.25-4.55-1.11-4.55-4.92 0-1.11.38-2 1.03-2.71-.1-.25-.45-1.29.1-2.64 0 0 .84-.27 2.75 1.02.79-.22 1.65-.33 2.5-.33.85 0 1.71.11 2.5.33 1.91-1.29 2.75-1.02 2.75-1.02.55 1.35.2 2.39.1 2.64.65.71 1.03 1.6 1.03 2.71 0 3.82-2.34 4.66-4.57 4.91.36.31.69.92.69 1.85V21c0 .27.16.59.67.5C19.14 20.16 22 16.42 22 12A10 10 0 0012 2z" />
          </svg>
          <span>GitHub</span>
        </a>
        <a
          href="https://github.com/dmytro-yemelianov/raps/releases"
          class="px-4 py-2 bg-primary-600 hover:bg-primary-500 text-white text-sm font-medium rounded-lg transition-colors"
        >
          Download
        </a>
      </div>
      
      <!-- Mobile menu button -->
      <button
        type="button"
        class="md:hidden p-2 text-slate-400 hover:text-white"
        aria-label="Toggle menu"
        id="mobile-menu-button"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>
  
  <!-- Mobile Navigation -->
  <div class="hidden md:hidden border-t border-slate-800" id="mobile-menu">
    <nav class="px-4 py-3 space-y-1">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            'block px-4 py-2 rounded-lg text-sm font-medium transition-colors',
            isActive(item.href)
              ? 'bg-slate-800 text-white'
              : 'text-slate-400 hover:text-white hover:bg-slate-800/50',
            item.label === 'Quick Start' && !isActive(item.href) && 'text-rapeseed-400',
            item.label === 'Docs' && !isActive(item.href) && 'text-emerald-400',
            item.label === 'API Coverage' && !isActive(item.href) && 'text-purple-400',
          ]}
        >
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>

<script>
  const button = document.getElementById('mobile-menu-button');
  const menu = document.getElementById('mobile-menu');
  
  button?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });
</script>

<!-- Spacer for fixed header -->
<div class="h-16"></div>
