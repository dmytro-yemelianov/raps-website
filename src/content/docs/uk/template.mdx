---
title: "Команди шаблонів"
description: "Управління шаблонами проєктів ACC"
section: "commands"
order: 11
icon: "📋"
---

# Команди шаблонів

Управління шаблонами проєктів в Autodesk Construction Cloud. Шаблони визначають стандартні конфігурації проєктів, які можна повторно використовувати при створенні нових проєктів, забезпечуючи однаковість по всьому портфелю.

> **Примітка:** Команди шаблонів потребують 3-legged OAuth з правами адміністратора облікового запису. Спочатку виконайте `raps auth login`.

## Огляд команд

| Команда | Опис |
|---------|------|
| `raps template list` | Показати доступні шаблони проєктів |
| `raps template info` | Переглянути деталі шаблону |
| `raps template create` | Створити новий шаблон |
| `raps template update` | Оновити існуючий шаблон |
| `raps template archive` | Архівувати шаблон |

---

## raps template list

Показати список доступних шаблонів проєктів в обліковому записі.

```bash
raps template list [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `--limit N` - Максимальна кількість шаблонів

**Приклад:**

```bash
$ raps template list --limit 10
```

---

## raps template info

Перегляд детальної інформації про конкретний шаблон.

```bash
raps template info <TEMPLATE_ID> [OPTIONS]
```

**Аргументи:**
- `TEMPLATE_ID` - Ідентифікатор шаблону

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)

**Приклад:**

```bash
$ raps template info "tmpl-abc123"
```

---

## raps template create

Створення нового шаблону проєкту.

```bash
raps template create [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `--name NAME` - Назва шаблону (обов'язково)

**Приклад:**

```bash
$ raps template create --name "Standard Commercial Project"
```

---

## raps template update

Оновлення існуючого шаблону проєкту.

```bash
raps template update <TEMPLATE_ID> [OPTIONS]
```

**Аргументи:**
- `TEMPLATE_ID` - Ідентифікатор шаблону

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `--name NAME` - Нова назва шаблону

**Приклад:**

```bash
$ raps template update "tmpl-abc123" --name "Updated Commercial Template"
```

---

## raps template archive

Архівування шаблону проєкту. Архівовані шаблони не можна використовувати для нових проєктів, але вони зберігаються для довідки.

```bash
raps template archive <TEMPLATE_ID> [OPTIONS]
```

**Аргументи:**
- `TEMPLATE_ID` - Ідентифікатор шаблону

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)

**Приклад:**

```bash
$ raps template archive "tmpl-abc123"
```

---

## Інструменти MCP

Усі операції з шаблонами доступні як інструменти MCP для інтеграції з ШІ-помічниками:

- `template_list` - Показати шаблони проєктів
- `template_info` - Отримати деталі шаблону
- `template_create` - Створити новий шаблон
- `template_update` - Оновити шаблон
- `template_archive` - Архівувати шаблон

```
You: "List all project templates in my account"
AI: Uses template_list tool to retrieve available templates
```
