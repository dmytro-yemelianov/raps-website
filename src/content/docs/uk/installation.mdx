---
title: "Встановлення"
description: "Встановлення RAPS CLI на Windows, macOS або Linux"
section: "getting-started"
order: 2
icon: "📥"
---

# Встановлення

RAPS CLI можна встановити кількома способами. Оберіть той, що найкраще відповідає вашим потребам.

## Швидке встановлення (рекомендовано)

Найшвидший спосіб розпочати роботу — скористатися нашими скриптами встановлення.

### Linux/macOS

```bash
curl -fsSL https://raw.githubusercontent.com/dmytro-yemelianov/raps/main/install.sh | bash
```

Цей скрипт:
- Автоматично визначає вашу ОС та архітектуру
- Завантажує правильний бінарний файл з GitHub Releases
- Перевіряє контрольну суму SHA256
- Встановлює у `~/.raps/bin`
- Оновлює ваш PATH (підтримує bash, zsh та fish)

**Опції:**
```bash
# Встановити конкретну версію
RAPS_VERSION=3.11.0 curl -fsSL .../install.sh | bash

# Власна директорія встановлення
RAPS_INSTALL_DIR=/opt/raps/bin curl -fsSL .../install.sh | bash

# Пропустити зміну PATH
RAPS_NO_MODIFY_PATH=1 curl -fsSL .../install.sh | bash

# Видалення
curl -fsSL .../install.sh | bash -s -- --uninstall
```

### Windows (PowerShell)

```powershell
irm https://raw.githubusercontent.com/dmytro-yemelianov/raps/main/install.ps1 | iex
```

Цей скрипт:
- Визначає вашу архітектуру (AMD64 або ARM64)
- Завантажує правильний бінарний файл з GitHub Releases
- Перевіряє контрольну суму SHA256
- Встановлює у `~\.raps\bin`
- Оновлює ваш User PATH

**Опції:**
```powershell
# Встановити конкретну версію
$env:RAPS_VERSION = "3.11.0"; irm .../install.ps1 | iex

# Або з параметрами (спочатку завантажте)
.\install.ps1 -Version "3.11.0"
.\install.ps1 -InstallDir "C:\Tools\raps"
.\install.ps1 -NoPathUpdate
.\install.ps1 -Uninstall
```

## pip (Python)

Якщо у вас встановлений Python 3.8+:

```bash
pip install raps
```

Це встановлює платформо-специфічні wheel-пакети, що містять бінарний файл RAPS.

**Доступні wheel-пакети:**
- Linux x64
- macOS x64 та ARM64 (Apple Silicon)
- Windows x64

> **Примітка:** Користувачам Linux ARM64 слід використовувати скрипт встановлення або завантажити бінарні файли безпосередньо.

### Python Bindings

Для програмного доступу з Python (не CLI) встановіть нативні прив'язки:

```bash
pip install raps-bindings
```

Це надає бібліотеку Python з повними анотаціями типів:

```python
from raps import Client

client = Client.from_env()
buckets = client.buckets.list()
```

📖 Дивіться [Python Bindings](/docs/python-bindings) для повної документації.

## Менеджери пакетів

### npm (Node.js)

```bash
npm install -g @dmytro-yemelianov/raps-cli
```

Або запуск без встановлення:

```bash
npx @dmytro-yemelianov/raps-cli --version
```

### Homebrew (macOS/Linux)

```bash
brew tap dmytro-yemelianov/raps
brew install raps
```

### Scoop (Windows)

```powershell
scoop bucket add raps https://github.com/dmytro-yemelianov/scoop-bucket
scoop install raps
```

### Cargo (Rust)

Якщо у вас встановлений Rust:

```bash
cargo install raps
```

**Вимоги:** Rust 1.88+ ([rustup.rs](https://rustup.rs/))

## Готові бінарні файли

Завантажте зі [сторінки релізів](https://github.com/dmytro-yemelianov/raps/releases):

| Платформа | Архітектура | Файл |
|-----------|-------------|------|
| Windows | x64 | `raps-cli-x86_64-pc-windows-msvc.zip` |
| macOS | Intel | `raps-cli-x86_64-apple-darwin.tar.xz` |
| macOS | Apple Silicon | `raps-cli-aarch64-apple-darwin.tar.xz` |
| Linux | x64 | `raps-cli-x86_64-unknown-linux-gnu.tar.xz` |
| Linux | ARM64 | `raps-cli-aarch64-unknown-linux-gnu.tar.xz` |

### Встановлення на Windows

```powershell
# Завантажити та розпакувати
Expand-Archive raps-cli-x86_64-pc-windows-msvc.zip -DestinationPath "$env:USERPROFILE\bin"

# Додати до PATH
$env:PATH += ";$env:USERPROFILE\bin"

# Зробити постійним
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";$env:USERPROFILE\bin", "User")
```

### Встановлення на macOS/Linux

```bash
# Розпакувати архів (формат .tar.xz)
tar -xJf raps-cli-*.tar.xz

# Перемістити до директорії у PATH
sudo mv raps /usr/local/bin/

# Зробити виконуваним
chmod +x /usr/local/bin/raps

# Перевірити встановлення
raps --version
```

## Docker

```bash
docker pull ghcr.io/dmytro-yemelianov/raps:latest

# Запуск зі змінними середовища
docker run --rm \
  -e APS_CLIENT_ID="your_id" \
  -e APS_CLIENT_SECRET="your_secret" \
  ghcr.io/dmytro-yemelianov/raps:latest auth test
```

## GitHub Actions

```yaml
- name: Install RAPS
  uses: dmytro-yemelianov/raps-action@v1

- name: Use RAPS
  run: raps auth test
  env:
    APS_CLIENT_ID: {'${{ secrets.APS_CLIENT_ID }}'}
    APS_CLIENT_SECRET: {'${{ secrets.APS_CLIENT_SECRET }}'}
```

## Збірка з вихідного коду

```bash
# Клонувати репозиторій
git clone https://github.com/dmytro-yemelianov/raps.git
cd raps

# Зібрати у режимі release
cargo build --release

# Бінарний файл знаходиться у target/release/raps
```

## Перевірка встановлення

```bash
raps --version
```

Ви повинні побачити подібне виведення:

```
raps 4.3.0
```

## Вирішення проблем

### "Permission denied" під час встановлення

Використайте власну директорію, до якої у вас є доступ на запис:

```bash
RAPS_INSTALL_DIR=$HOME/bin ./install.sh
```

### "Command not found" після встановлення

Можливо, ваша оболонка не перезавантажила PATH. Спробуйте:

```bash
# Bash
source ~/.bashrc

# Zsh
source ~/.zshrc

# Fish
source ~/.config/fish/config.fish
```

Або відкрийте нове вікно терміналу.

### Політика виконання PowerShell

Якщо ви отримуєте помилку політики виконання:

```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

### Невдала перевірка контрольної суми

Зазвичай це означає, що завантаження було пошкоджене. Спробуйте ще раз або завантажте вручну зі [сторінки релізів](https://github.com/dmytro-yemelianov/raps/releases).

## Наступні кроки

Після встановлення [налаштуйте свої облікові дані](/docs/configuration).
