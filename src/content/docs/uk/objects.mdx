---
title: "Команди Object"
description: "Завантаження, скачування та керування об'єктами в OSS-бакетах"
section: "commands"
order: 3
icon: "📄"
---

# Команди Object

Завантаження, скачування та керування об'єктами в OSS-бакетах.

![Демонстрація операцій з об'єктами RAPS](/terminal/oss-demo.gif)

## Огляд команд

| Команда | Опис |
|---------|------|
| `raps object upload` | Завантажити файли до бакета |
| `raps object download` | Скачати об'єкти |
| `raps object list` | Переглянути список об'єктів у бакеті |
| `raps object details` | Отримати деталі об'єкта |
| `raps object delete` | Видалити об'єкт |
| `raps object signed-url` | Згенерувати підписаний URL для скачування |
| `raps object urn` | Отримати URN об'єкта для трансляції |

## raps object upload

Завантаження файлів до OSS-бакета з відстеженням прогресу.

```bash
raps object upload <bucket-key> <file-path> [options]
```

**Параметри:**
- `--key, -k`: Власний ключ об'єкта (за замовчуванням -- ім'я файлу)
- `--batch`: Завантаження кількох файлів
- `--parallel`: Увімкнути паралельне завантаження
- `--concurrency`: Кількість одночасних завантажень (за замовчуванням: 5)

**Завантаження одного файлу:**

```bash
$ raps object upload my-bucket model.rvt
Uploading model.rvt...
████████████████████████████████████████ 100% (45.2 MB)
✓ Upload complete!
  Object Key: model.rvt
  URN: dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6bXktYnVja2V0L21vZGVsLnJ2dA
```

**Пакетне завантаження з шаблонами:**

```bash
$ raps object upload my-bucket ./models/*.dwg --batch --parallel
Uploading 5 files with 5 concurrent uploads...
  [1/5] floor-plan.dwg ████████████████████ 100%
  [2/5] elevation.dwg ████████████████████ 100%
  [3/5] section-a.dwg ████████████████████ 100%
  [4/5] section-b.dwg ████████████████████ 100%
  [5/5] details.dwg ████████████████████ 100%
✓ All files uploaded successfully!
```

**Власний ключ об'єкта:**

```bash
raps object upload my-bucket model.rvt --key "projects/2024/building-a.rvt"

### Продуктивність завантаження (v3.7.0+)

RAPS v3.7.0 впроваджує значні покращення продуктивності завантаження файлів:

- **🚀 Паралельне багаточастинне завантаження**: у 3-10 разів швидше для великих файлів (>5 МБ)
- **💾 Потокове завантаження**: зменшене використання пам'яті, без обмежень розміру файлу
- **🔄 Автоматичне повторення**: послідовна логіка повторних спроб для всіх операцій завантаження
- **📊 Розумна конкурентність**: оптимізована паралельна обробка з обмеженою конкурентністю

**Порівняння продуктивності для файлу 500 МБ:**
- **v3.6.x**: ~15 хвилин (послідовне завантаження)
- **v3.7.0+**: ~2-3 хвилини (паралельне завантаження)

**Найкращі практики для великих файлів:**
```bash
# Великі файли автоматично використовують паралельне багаточастинне завантаження
raps object upload my-bucket large-model.rvt
# ✓ Автоматично розбиває на частини по 5 МБ
# ✓ Завантажує 5 частин одночасно
# ✓ Обробляє повторні спроби при помилці частини
# ✓ Відновлює при розриві з'єднання
```

## raps object download

Скачування об'єктів з бакета.

```bash
raps object download <bucket-key> <object-key> [--output PATH]
```

```bash
$ raps object download my-bucket model.rvt --output ./downloads/
Downloading model.rvt...
████████████████████████████████████████ 100% (45.2 MB)
✓ Downloaded to: ./downloads/model.rvt
```

## raps object list

Перегляд усіх об'єктів у бакеті.

```bash
$ raps object list my-bucket
Objects in 'my-bucket':
┌────────────────────────────┬──────────────┬──────────────────────┐
│ Key                        │ Size         │ Last Modified        │
├────────────────────────────┼──────────────┼──────────────────────┤
│ model.rvt                  │ 45.2 MB      │ 2024-01-15 10:30:00  │
│ floor-plan.dwg             │ 2.1 MB       │ 2024-01-15 10:32:00  │
│ elevation.dwg              │ 1.8 MB       │ 2024-01-15 10:33:00  │
└────────────────────────────┴──────────────┴──────────────────────┘
```

**З фільтром за префіксом:**

```bash
raps object list my-bucket --prefix "projects/2024/"
```

## raps object signed-url

Генерація попередньо підписаного S3 URL для прямого скачування.

```bash
raps object signed-url <bucket-key> <object-key> [--minutes MINUTES]
```

```bash
$ raps object signed-url my-bucket model.rvt --minutes 60
Signed URL (expires in 60 minutes):
https://developer.api.autodesk.com/oss/v2/signedresources/...
```

**Випадки використання:**
- Поширення тимчасових посилань для скачування
- Інтеграція із зовнішніми системами
- Забезпечення прямого скачування клієнтами

## raps object urn

Отримання Base64-закодованого URN об'єкта (необхідний для трансляції).

```bash
$ raps object urn my-bucket model.rvt
URN: dXJuOmFkc2sub2JqZWN0czpvcy5vYmplY3Q6bXktYnVja2V0L21vZGVsLnJ2dA
```

**Збереження у змінну:**

```bash
URN=$(raps object urn my-bucket model.rvt --output plain)
raps translate start "$URN" --format svf2
```

## raps object delete

Видалення об'єкта з бакета.

```bash
$ raps object delete my-bucket model.rvt
⚠ Warning: This will permanently delete 'model.rvt'
  Are you sure? (y/N): y
✓ Object deleted successfully
```

## Завантаження великих файлів

Для файлів понад 100 МБ RAPS автоматично використовує відновлюване багаточастинне завантаження:

```bash
$ raps object upload my-bucket large-model.ifc
Uploading large-model.ifc (2.1 GB)...
Using resumable upload (21 parts)...
████████████████████████████████████████ 100%
✓ Upload complete!
```

**Переваги:**
- Відновлення перерваних завантажень
- Краща продуктивність для великих файлів
- Автоматичне повторення при помилках

## Приклади CI/CD

### Завантаження та отримання URN

```bash
raps object upload my-bucket model.rvt
URN=$(raps object urn my-bucket model.rvt --output plain)
echo "Uploaded with URN: $URN"
```

### Пакетне завантаження цілої директорії

```bash
find ./models -name "*.dwg" | xargs -I {} raps object upload my-bucket {}
```

## Наступні кроки

- [Трансляція](/docs/translation) -- Конвертація завантажених моделей
- [Керування даними](/docs/data-management) -- Робота з BIM 360/ACC
