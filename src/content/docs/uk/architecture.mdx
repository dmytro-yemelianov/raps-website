---
title: "Архітектура"
description: "Мікроядерна архітектура RAPS"
section: "getting-started"
order: 3
icon: "🏗️"
---

# Архітектура

RAPS побудований на **мікроядерній архітектурі**, натхненній принципами проєктування Unix, що забезпечує безпеку, тестованість та незалежний розвиток функціоналу.

## Філософія проєктування

Мікроядерна архітектура розділяє RAPS на:

1. **Мінімальне ядро**: Основна функціональність компактна, перевірена та надійна
2. **Передача повідомлень**: Компоненти взаємодіють через чітко визначені інтерфейси
3. **Ізоляція**: Збої у розширеннях не призводять до аварії ядра
4. **Розширюваність**: Функції додаються через плагіни, а не модифікації ядра
5. **Безпека**: Поверхня атаки мінімізована в довіреному ядрі

## Рівні архітектури

<div class="not-prose my-8">
  {/* User Interfaces Layer */}
  <div class="relative">
    <div class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">User Interfaces</div>
    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-2 p-4 bg-slate-800/50 rounded-lg border border-slate-700">
      <div class="flex flex-col items-center p-3 bg-slate-900 rounded-md border border-slate-600 hover:border-blue-500/50 transition-colors">
        <span class="text-sm font-semibold text-white">CLI</span>
        <span class="text-xs text-slate-400">clap</span>
      </div>
      <div class="flex flex-col items-center p-3 bg-slate-900 rounded-md border border-slate-600 hover:border-purple-500/50 transition-colors">
        <span class="text-sm font-semibold text-white">MCP Server</span>
        <span class="text-xs text-slate-400">rmcp</span>
      </div>
      <div class="flex flex-col items-center p-3 bg-slate-900 rounded-md border border-slate-600 hover:border-green-500/50 transition-colors">
        <span class="text-sm font-semibold text-white">GitHub Action</span>
        <span class="text-xs text-slate-400">yaml</span>
      </div>
      <div class="flex flex-col items-center p-3 bg-slate-900 rounded-md border border-slate-600 hover:border-cyan-500/50 transition-colors">
        <span class="text-sm font-semibold text-white">Docker</span>
        <span class="text-xs text-slate-400">container</span>
      </div>
      <div class="flex flex-col items-center p-3 bg-slate-900 rounded-md border border-slate-600 hover:border-amber-500/50 transition-colors">
        <span class="text-sm font-semibold text-white">TUI</span>
        <span class="text-xs text-slate-400">ratatui</span>
      </div>
    </div>
  </div>

  {/* Arrow Down */}
  <div class="flex justify-center my-2">
    <svg class="w-6 h-6 text-slate-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>

  {/* RAPS Application Layer */}
  <div class="relative">
    <div class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">RAPS Application</div>
    <div class="p-4 bg-gradient-to-b from-blue-900/30 to-blue-950/30 rounded-lg border border-blue-800/50">
      <div class="flex flex-col sm:flex-row gap-3 justify-center items-center">
        <div class="flex flex-col items-center p-4 bg-slate-900 rounded-md border border-blue-600/50 min-w-[160px]">
          <span class="text-sm font-bold text-blue-400">raps</span>
          <span class="text-xs text-slate-400">Main Application</span>
        </div>
        <svg class="w-5 h-5 text-slate-500 rotate-90 sm:rotate-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
        <div class="flex flex-col items-center p-4 bg-slate-900 rounded-md border-2 border-amber-500/50 min-w-[160px]">
          <span class="text-sm font-bold text-amber-400">raps-kernel</span>
          <span class="text-xs text-slate-400">Core Kernel</span>
        </div>
      </div>
    </div>
  </div>

  {/* Arrow Down */}
  <div class="flex justify-center my-2">
    <svg class="w-6 h-6 text-slate-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>

  {/* Service Crates Layer */}
  <div class="relative">
    <div class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Service Crates</div>
    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 p-4 bg-slate-800/50 rounded-lg border border-slate-700">
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-oss</code>
      </div>
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-derivative</code>
      </div>
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-dm</code>
      </div>
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-da</code>
      </div>
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-acc</code>
      </div>
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-webhooks</code>
      </div>
      <div class="flex items-center justify-center p-3 bg-slate-900 rounded-md border border-emerald-600/30 hover:border-emerald-500/50 transition-colors">
        <code class="text-xs text-emerald-400">raps-reality</code>
      </div>
    </div>
  </div>
</div>

## Ядро (`raps-kernel`)

Ядро -- це мінімальна довірена основа, на якій будується весь функціонал.

**Вміст:**
- Автентифікація (OAuth2, SSA, управління токенами)
- HTTP-клієнт (з логікою повторних спроб та middleware)
- Управління конфігурацією
- Безпечне зберігання облікових даних (системне сховище ключів)
- Доменні типи (URN, BucketKey, ObjectKey)
- Обробка помилок з кодами завершення
- Логування з редагуванням секретів

**Обмеження:**
- **Розмір**: Менше 3000 рядків коду (ціль: ~2000 LOC)
- **Безпека**: `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`
- **Покриття тестами**: Понад 90% на критичних шляхах
- **Залежності**: Мінімум зовнішніх залежностей

**Продуктивність збірки:**
- Інкрементальна перевірка: менше 5 с
- Повна збірка: менше 30 с

## Сервісні крейти

Сервісні крейти забезпечують функціональність APS API:

- **`raps-oss`**: Object Storage Service (бакети, об'єкти, завантаження, вивантаження)
- **`raps-derivative`**: Model Derivative API (трансляції, маніфести, деривативи)
- **`raps-dm`**: Data Management API (хаби, проєкти, папки, елементи, версії)
- **`raps-da`**: Design Automation API (рушії, активності, робочі елементи)
- **`raps-acc`**: ACC/BIM 360 (проблеми, RFI, активи, подання, чек-листи)
- **`raps-webhooks`**: Webhooks API (підписки, події, верифікація)
- **`raps-reality`**: Reality Capture API (фотосцени, обробка, результати)

**Принципи сервісів:**
- Залежать лише від `raps-kernel`, а не один від одного
- Незалежні домени відмов
- Можуть тестуватися ізольовано
- Можуть розвиватися незалежно

## Можливості

RAPS забезпечує повне покриття API Autodesk Platform Services:

### Основні сервіси
- **Автентифікація**: 2-legged OAuth, 3-legged OAuth, Device Code Flow, SSA (Secure Service Accounts)
- **Object Storage (OSS)**: CRUD бакетів, CRUD об'єктів, паралельне завантаження, Signed URLs (S3)
- **Model Derivative**: Завдання трансляції, статус та маніфест, завантаження деривативів
- **Data Management**: Хаби та проєкти, папки та елементи, остання версія та деривативи

### Розширені можливості
- **Account Admin API**: Управління обліковими записами та адміністрування
- **Модулі ACC**: Issues, RFI, Assets, Submittals, Checklists
- **Design Automation**: Управління рушіями, активностями, робочими елементами
- **Reality Capture**: Можливості фотограмметричної обробки
- **Webhooks**: Підписки на події та автоматизація робочих процесів
- **Pipelines**: Автоматизація робочих процесів через YAML/JSON
- **Plugins**: Розширення функціоналу власними командами
- **MCP Server**: Інтеграція з AI-асистентами через Model Context Protocol
- **TUI**: Термінальний інтерфейс для інтерактивних операцій

## Команди збірки

```bash
# Стандартна збірка (включає всі можливості)
cargo build

# Мінімальна збірка (лише ядро + основні сервіси)
cargo build --no-default-features --features core
```

## Вивід версії

```bash
$ raps --version
raps 4.14.0
```

## Цільові показники продуктивності

RAPS оптимізований для швидкого циклу зворотного зв'язку під час розробки:

| Метрика | Ціль | Вимірювання |
|---------|------|-------------|
| Перевірка ядра (інкрементальна) | Менше 5 с | Після зміни одного файлу |
| Перевірка workspace (інкрементальна) | Менше 30 с | Після зміни одного файлу |
| Запуск CLI (`--help`) | Менше 100 мс | Холодний старт |
| Завантаження файлу (100 МБ, паралельно) | Менше 30 с | Залежить від мережі (100 Мбіт/с) |
| Виклик MCP tool | Менше 200 мс | Інтеграція з AI-асистентом |

**Інструменти збірки:**
- **Windows**: лінкер `lld-link` (через LLVM)
- **Linux**: лінкер `mold` (у CI)
- **Усі платформи**: `sccache` для кешування компіляції, `cargo-nextest` для паралельних тестів

Дивіться [Налаштування середовища розробки](/docs/development) для локальної конфігурації.

## Переваги

**Безпека:**
- Мінімальна поверхня атаки в ядрі
- Незалежний аудит безпеки сервісів
- Чіткі межі безпеки

**Тестованість:**
- Ядро може тестуватися ізольовано
- Сервіси можуть бути замоковані для інтеграційних тестів
- Досяжне високе покриття тестами

**Підтримуваність:**
- Чіткий розподіл відповідальностей
- Незалежний розвиток функціоналу
- Простіший код-рев'ю

**Продуктивність:**
- Швидкі інкрементальні збірки
- Паралельне виконання тестів
- Оптимізоване використання лінкера

**Бізнес-модель:**
- Основний функціонал залишається відкритим
- Корпоративні можливості забезпечують сталий розвиток
- Чіткий шлях оновлення для користувачів
