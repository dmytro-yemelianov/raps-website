---
title: "Трансляція"
description: "Трансляція CAD-файлів у формати для перегляду та завантаження"
section: "commands"
order: 4
icon: "🔄"
---

# Команди трансляції

Трансляція CAD-файлів у різні формати за допомогою Model Derivative API.

![Демо трансляції моделей RAPS](/terminal/model-derivative-demo.gif)

## Огляд команд

| Команда | Опис |
|---------|------|
| `raps translate start` | Запустити завдання трансляції |
| `raps translate status` | Перевірити статус трансляції |
| `raps translate manifest` | Переглянути маніфест трансляції |
| `raps translate download` | Завантажити деривативи |
| `raps translate delete` | Видалити маніфест |

## Підтримувані формати

### Вхідні формати

RAPS підтримує всі сумісні з APS вхідні формати:
- **Autodesk**: RVT, DWG, DWF, NWD, NWC, DGN
- **Індустріальні**: IFC, STEP, IGES, SAT
- **3D**: OBJ, STL, 3DS, FBX, GLTF
- **Та інші...**

### Вихідні формати

| Формат | Опис |
|--------|------|
| `svf2` | Потоковий формат для Viewer (рекомендовано) |
| `svf` | Застарілий потоковий формат |
| `thumbnail` | Мініатюри у форматі PNG |
| `obj` | Wavefront OBJ сітка |
| `stl` | Формат сітки STL |
| `step` | Формат CAD STEP |
| `iges` | Формат CAD IGES |
| `ifc` | Формат BIM IFC |

## raps translate start

Запуск завдання трансляції для завантаженого файлу.

```bash
raps translate start <urn> --format <format> [options]
```

**Опції:**
- `--format, -f`: Вихідний формат (обов'язково)
- `--wait, -w`: Очікувати завершення
- `--root-filename`: Кореневий файл для ZIP-архівів
- `--force`: Примусова повторна трансляція

**Базова трансляція:**

```bash
$ raps translate start dXJuOmFkc2sub2JqZWN0cy... --format svf2
✓ Translation started
  URN: dXJuOmFkc2sub2JqZWN0cy...
  Status: pending
```

**Очікування завершення:**

```bash
$ raps translate start $URN --format svf2 --wait
Translation in progress...
  ████████████████████████████████████████ 100%
✓ Translation complete (47s)
```

**Декілька форматів:**

```bash
raps translate start $URN --format svf2
raps translate start $URN --format obj
raps translate start $URN --format thumbnail
```

## raps translate status

Перевірка статусу завдання трансляції.

```bash
$ raps translate status $URN
Translation Status:
  URN: dXJuOmFkc2sub2JqZWN0cy...
  Status: success
  Progress: 100%

  Derivatives:
    • svf2 (3D views, 2D sheets)
    • thumbnail (256x256)
```

**Очікування завершення:**

```bash
$ raps translate status $URN --wait
Waiting for translation...
████████████████████████████████████████ 100%
✓ Translation complete!
```

**JSON-вивід для скриптів:**

```bash
raps translate status $URN --output json | jq '.status'
```

## raps translate manifest

Перегляд повного маніфесту трансляції з усіма деривативами.

```bash
$ raps translate manifest $URN
Manifest for: dXJuOmFkc2sub2JqZWN0cy...
Status: success

Derivatives:
┌─────────────┬────────────────────────┬────────────┐
│ Type        │ Role                   │ Status     │
├─────────────┼────────────────────────┼────────────┤
│ svf2        │ 3d                     │ success    │
│ svf2        │ 2d                     │ success    │
│ thumbnail   │ thumbnail              │ success    │
└─────────────┴────────────────────────┴────────────┘
```

## raps translate download

Завантаження файлів деривативів (OBJ, STL, STEP тощо).

```bash
raps translate download <urn> --output <directory>
```

```bash
$ raps translate download $URN --output ./derivatives/
Downloading derivatives...
  [1/3] model.obj ████████████████████ 100%
  [2/3] model.mtl ████████████████████ 100%
  [3/3] textures.zip ████████████████████ 100%
✓ Downloaded to: ./derivatives/
```

## Повний робочий процес

### Завантаження та трансляція

```bash
# 1. Створити бакет
raps bucket create --key my-project --policy persistent --region US

# 2. Завантажити файл
raps object upload my-project building.rvt

# 3. Отримати URN
URN=$(raps object urn my-project building.rvt --output plain)

# 4. Запустити трансляцію
raps translate start "$URN" --format svf2 --wait

# 5. Перевірити маніфест
raps translate manifest "$URN"
```

### Пакетний пайплайн трансляції

```bash
for file in ./models/*.dwg; do
  name=$(basename "$file")
  raps object upload my-bucket "$file"
  URN=$(raps object urn my-bucket "$name" --output plain)
  raps translate start "$URN" --format svf2
  echo "Started translation for $name"
done
```

## Пресети трансляції

Використовуйте пресети для типових робочих процесів:

```bash
# Висока якість для презентацій
raps translate start $URN --format svf2 --preset high-quality

# Швидкий попередній перегляд
raps translate start $URN --format svf2 --preset fast
```

## Усунення неполадок

### Помилка "Translation failed"

1. Перевірте маніфест для отримання детальних повідомлень про помилки:
   ```bash
   raps translate manifest $URN --output json
   ```
2. Переконайтеся, що вихідний файл валідний
3. Перевірте, що формат файлу підтримується

### Помилка "Derivative not found"

1. Переконайтеся, що трансляція завершилася успішно
2. Перевірте маніфест на наявність доступних деривативів
3. Дочекайтеся завершення трансляції, використовуючи `--wait`

## Наступні кроки

- [Керування даними](/docs/data-management) — Робота з проєктами BIM 360/ACC
- [Design Automation](/docs/design-automation) — Автоматизація обробки CAD
