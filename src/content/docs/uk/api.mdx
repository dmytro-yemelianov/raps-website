---
title: "Спеціальні API-запити"
description: "Виконання довільних HTTP-запитів до будь-якого ендпоінту APS"
section: "commands"
order: 9
icon: "🔌"
---

# Спеціальні API-запити

Виконання довільних HTTP-запитів до будь-якого ендпоінту APS. Корисно для доступу до API, які ще не обгорнуті командами RAPS, тестування ендпоінтів або автоматизації власних робочих процесів.

> **Нове у v4.5.0**

## Огляд команд

| Команда | Опис |
|---------|------|
| `raps api get` | Надіслати GET-запит |
| `raps api post` | Надіслати POST-запит |
| `raps api put` | Надіслати PUT-запит |
| `raps api patch` | Надіслати PATCH-запит |
| `raps api delete` | Надіслати DELETE-запит |

---

## raps api get

Надсилання GET-запиту до ендпоінту APS.

```bash
raps api get <ENDPOINT> [OPTIONS]
```

**Аргументи:**
- `ENDPOINT` - Шлях API-ендпоінту (наприклад, `/oss/v2/buckets`) або повний URL

**Опції:**
- `--query KEY=VALUE` - Додати параметри запиту (можна повторювати)
- `-H, --header KEY:VALUE` - Додати власні заголовки (можна повторювати)
- `-o, --output FILE` - Зберегти тіло відповіді у файл
- `-v, --verbose` - Показати заголовки та статус відповіді

**Приклади:**

```bash
# Отримати профіль поточного користувача
raps api get /userprofile/v1/users/@me

# Показати хаби з детальним виведенням
raps api get /project/v1/hubs --verbose

# Зберегти відповідь у файл
raps api get /oss/v2/buckets --output buckets.json
```

---

## raps api post

Надсилання POST-запиту з необов'язковим JSON-тілом.

```bash
raps api post <ENDPOINT> [OPTIONS]
```

**Аргументи:**
- `ENDPOINT` - Шлях API-ендпоінту або повний URL

**Опції:**
- `-d, --data JSON` - JSON-тіло запиту у командному рядку
- `-f, --data-file FILE` - Зчитати тіло запиту з файлу
- `--query KEY=VALUE` - Додати параметри запиту (можна повторювати)
- `-H, --header KEY:VALUE` - Додати власні заголовки (можна повторювати)
- `-o, --output FILE` - Зберегти тіло відповіді у файл
- `-v, --verbose` - Показати заголовки та статус відповіді

> **Примітка:** `--data` та `--data-file` є взаємовиключними.

**Приклади:**

```bash
# Створити бакет з JSON у командному рядку
raps api post /oss/v2/buckets \
  -d '{"bucketKey":"my-bucket","policyKey":"transient"}'

# Надіслати тіло з файлу
raps api post /issues/v1/containers/PROJECT_ID/quality-issues \
  --data-file issue.json
```

---

## raps api put

Надсилання PUT-запиту для заміни ресурсу.

```bash
raps api put <ENDPOINT> [OPTIONS]
```

Ті самі опції, що й у `raps api post`.

---

## raps api patch

Надсилання PATCH-запиту для часткового оновлення ресурсу.

```bash
raps api patch <ENDPOINT> [OPTIONS]
```

Ті самі опції, що й у `raps api post`.

**Приклад:**

```bash
# Оновити статус вебхука
raps api patch /webhooks/v1/systems/data/events/dm.version.added/hooks/HOOK_ID \
  -d '{"status":"inactive"}'
```

---

## raps api delete

Надсилання DELETE-запиту для видалення ресурсу.

```bash
raps api delete <ENDPOINT> [OPTIONS]
```

**Опції:**
- `--query KEY=VALUE` - Додати параметри запиту (можна повторювати)
- `-H, --header KEY:VALUE` - Додати власні заголовки (можна повторювати)
- `-v, --verbose` - Показати заголовки та статус відповіді

> **Примітка:** DELETE-запити не підтримують тіло запиту.

**Приклад:**

```bash
raps api delete /oss/v2/buckets/my-bucket
```

---

## Інструмент MCP

Інструмент MCP `api_request` дозволяє ШІ-помічникам виконувати довільні API-запити:

```
You: "Call the APS user profile API to get my account details"
AI: Uses api_request tool with GET /userprofile/v1/users/@me
```
