---
title: "Команди бакетів"
description: "Управління бакетами OSS для зберігання файлів"
section: "commands"
order: 2
icon: "📦"
---

# Команди бакетів

Управління бакетами Object Storage Service (OSS) для зберігання файлів.

![RAPS bucket operations demo](/terminal/oss-demo.gif)

## Огляд команд

| Команда | Опис |
|---------|------|
| `raps bucket create` | Створити новий бакет |
| `raps bucket list` | Показати список усіх бакетів |
| `raps bucket details` | Отримати деталі бакета |
| `raps bucket delete` | Видалити бакет |

## raps bucket create

Інтерактивне створення нового OSS-бакета.

```bash
raps bucket create [--key KEY] [--policy POLICY] [--region REGION]
```

**Опції:**
- `--key, -k`: Ключ бакета (буде запитано, якщо не вказано)
- `--policy, -p`: Політика зберігання: `transient`, `temporary` або `persistent`
- `--region, -r`: Регіон: `US` або `EMEA`

**Інтерактивний приклад:**

```bash
$ raps bucket create
Note: Bucket keys must be globally unique across all APS applications.
Suggestion: Use a prefix like 'aps-1234567890-yourname'
Enter bucket key: aps-1234567890-mybucket
Select region:
  > US
    EMEA
Select retention policy:
  > transient (deleted after 24 hours)
    temporary (deleted after 30 days)
    persistent (kept until deleted)

✓ Bucket created successfully!
  Key: aps-1234567890-mybucket
  Policy: transient
```

**Неінтерактивний приклад:**

```bash
$ raps bucket create --key my-bucket --policy persistent --region US
✓ Bucket created successfully!
```

### Політики зберігання

| Політика | Опис |
|----------|------|
| `transient` | Автоматично видаляється через 24 години |
| `temporary` | Автоматично видаляється через 30 днів |
| `persistent` | Зберігається до ручного видалення |

### Правила ключів бакетів

- Повинен бути глобально унікальним серед усіх застосунків APS
- 3-128 символів
- Лише малі літери, цифри, дефіси, підкреслення та крапки

## raps bucket list

Показати список усіх бакетів з усіх регіонів.

```bash
$ raps bucket list
Fetching buckets from all regions...

US Region:
┌────────────────────────────────────┬────────────┬──────────────────────┐
│ Key                                │ Policy     │ Created              │
├────────────────────────────────────┼────────────┼──────────────────────┤
│ my-app-models                      │ persistent │ 2024-01-10 10:30:00  │
│ temp-uploads-12345                 │ transient  │ 2024-01-15 14:22:00  │
└────────────────────────────────────┴────────────┴──────────────────────┘

EMEA Region:
┌────────────────────────────────────┬────────────┬──────────────────────┐
│ Key                                │ Policy     │ Created              │
├────────────────────────────────────┼────────────┼──────────────────────┤
│ eu-project-files                   │ persistent │ 2024-01-08 09:15:00  │
└────────────────────────────────────┴────────────┴──────────────────────┘
```

**Вивід у форматі JSON:**

```bash
raps bucket list --output json
```

## raps bucket details

Отримання детальної інформації про конкретний бакет.

```bash
$ raps bucket details my-bucket
Bucket Details:
  Key: my-bucket
  Policy: persistent
  Owner: abc123xyz
  Created: 2024-01-10 10:30:00 UTC
```

## raps bucket delete

Видалення порожнього бакета.

```bash
$ raps bucket delete my-bucket
⚠ Warning: This will permanently delete bucket 'my-bucket'
  Are you sure? (y/N): y
✓ Bucket deleted successfully
```

**Примусове видалення (без підтвердження):**

```bash
raps bucket delete my-bucket --force
```

> **Примітка:** Бакети повинні бути порожніми перед видаленням. Спочатку видаліть усі об'єкти.

## Приклади CI/CD

### Створення бакета з унікальною назвою

```bash
BUCKET_KEY="app-$(date +%s)-$CI_JOB_ID"
raps bucket create --key "$BUCKET_KEY" --policy transient --region US
```

### Список бакетів у форматі JSON для обробки

```bash
raps bucket list --output json | jq '.[] | select(.policy == "persistent")'
```

## Наступні кроки

- [Операції з об'єктами](/docs/objects) -- Завантаження та управління файлами
- [Трансляція](/docs/translation) -- Конвертація моделей у формати для перегляду
