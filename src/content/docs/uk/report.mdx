---
title: "Портфельна звітність"
description: "Агреговані звіти по кількох проєктах ACC/BIM 360"
section: "commands"
order: 10
icon: "📊"
---

# Портфельна звітність

Генерація агрегованих звітів по всьому портфелю ACC/BIM 360. Ці команди підсумовують RFI, Issues, Submittals, Checklists та Assets по кількох проєктах, надаючи загальний огляд вашої будівельної програми.

> **Примітка:** Команди звітності потребують 3-legged OAuth з правами адміністратора облікового запису. Спочатку виконайте `raps auth login`.

## Огляд команд

| Команда | Опис |
|---------|------|
| `raps report rfi-summary` | Агрегований звіт по RFI |
| `raps report issues-summary` | Агрегований звіт по Issues |
| `raps report submittals-summary` | Агрегований звіт по Submittals |
| `raps report checklists-summary` | Агрегований звіт по Checklists |
| `raps report assets-summary` | Агрегований звіт по Assets |

---

## raps report rfi-summary

Генерація агрегованого підсумку RFI по кількох проєктах.

```bash
raps report rfi-summary [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `-f, --filter EXPRESSION` - Вираз фільтрації проєктів (наприклад, `status:active,name:*Hospital*`)
- `--status STATUS` - Фільтрувати RFI за статусом
- `--since YYYY-MM-DD` - Включати лише RFI, створені після цієї дати

**Приклад:**

```bash
# Підсумок RFI по всіх активних проєктах з січня 2025
$ raps report rfi-summary --filter "status:active" --since 2025-01-01
```

---

## raps report issues-summary

Генерація агрегованого підсумку Issues по кількох проєктах.

```bash
raps report issues-summary [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `-f, --filter EXPRESSION` - Вираз фільтрації проєктів (наприклад, `status:active,name:*Hospital*`)
- `--status STATUS` - Фільтрувати Issues за статусом
- `--since YYYY-MM-DD` - Включати лише Issues, створені після цієї дати

**Приклад:**

```bash
# Підсумок відкритих Issues по проєктах з назвою "Tower"
$ raps report issues-summary --filter "name:*Tower*" --status open
```

---

## raps report submittals-summary

Генерація агрегованого підсумку Submittals по кількох проєктах.

```bash
raps report submittals-summary [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `-f, --filter EXPRESSION` - Вираз фільтрації проєктів
- `--status STATUS` - Фільтрувати Submittals за статусом

**Приклад:**

```bash
# Підсумок Submittals, що очікують на перегляд
$ raps report submittals-summary --status pending_review
```

---

## raps report checklists-summary

Генерація агрегованого підсумку Checklists по кількох проєктах.

```bash
raps report checklists-summary [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `-f, --filter EXPRESSION` - Вираз фільтрації проєктів
- `--status STATUS` - Фільтрувати Checklists за статусом

**Приклад:**

```bash
# Підсумок Checklists по активних проєктах
$ raps report checklists-summary --filter "status:active"
```

---

## raps report assets-summary

Генерація агрегованого підсумку Assets по кількох проєктах.

```bash
raps report assets-summary [OPTIONS]
```

**Опції:**
- `-a, --account ACCOUNT_ID` - Ідентифікатор облікового запису ACC (за замовчуванням: змінна середовища `APS_ACCOUNT_ID`)
- `-f, --filter EXPRESSION` - Вираз фільтрації проєктів

**Приклад:**

```bash
# Підсумок Assets по всіх проєктах
$ raps report assets-summary
```

---

## Інструменти MCP

Наступні інструменти MCP дозволяють ШІ-помічникам генерувати портфельні звіти:

- `report_rfi_summary` - Генерація підсумку RFI по проєктах
- `report_issues_summary` - Генерація підсумку Issues по проєктах

```
You: "Summarize all open RFIs across my active projects"
AI: Uses report_rfi_summary tool with filter "status:active"
```
